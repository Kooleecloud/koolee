/**
 * Retired with the 7→4 step merge: the standalone ZIP step folded into the
 * flight step. The ZIP field now lives in app/book/flight/page.tsx and the
 * out-of-coverage swap in components/coverage-step-form.tsx.
 *
 * This stub (and components/address-step-form.tsx, replaced by
 * coverage-step-form.tsx / pickup-step-form.tsx) is kept only pending
 * deletion approval.
 */
export {};
